-Use database to fetch to-do-list
    -Db columns: id, note, ANYTHING ELSE?
    -Initial webpage will show all notes
    -User enters new note, add to db
    -User deletes note, remove from db
    -User updates note, modify in db
-Search bar always at top of webpage
-New note button always on webpage
-Add check box
    -Add Completed Section
    -When check box is checked, move task to Completed Section with strikethrough text
-Add "x" to remove task
    -Remove from database when clicked